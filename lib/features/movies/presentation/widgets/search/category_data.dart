import 'package:flutter/material.dart';

class CategoryData {
  final String title;
  final String endpoint;
  final IconData icon;
  final String subtitle;

  const CategoryData(this.title, this.endpoint, this.icon, this.subtitle);
}
